# 两颗星球：本地启动说明

## 需要准备

- Node.js 22.13 或更高版本
- pnpm

## 启动

1. 安装依赖：`pnpm install`
2. 复制 `.dev.vars.example` 为 `.dev.vars`
3. 将 `COUPLE_CODE` 改成你们自己的暗号
4. 启动开发环境：`pnpm dev`
5. 浏览器打开终端显示的本地地址

## 构建检查

运行 `pnpm build`。

## 数据

- 双人状态、想念、心愿、相簿和日期设置保存在 D1 数据库中。
- 数据表结构位于 `db/schema.ts`。
- 数据库迁移位于 `drizzle/`。
- `.dev.vars`、本地数据库、依赖目录和版本历史不会包含在导出包中。

## 当前基础功能

- 双人暗号进入和身份选择
- 心情状态
- “想你”记录
- 恋爱天数与见面倒计时
- 恋爱起始 / 下次见面日期设置（支持模糊日期）
- 纪念日自动倒数
- 首页相簿（滑动切换、圆角、可添加 / 删除照片）
- 共同心愿
- iPhone 主屏幕 PWA

生产环境的访问暗号需要在对应托管平台中单独设置为 `COUPLE_CODE`。
